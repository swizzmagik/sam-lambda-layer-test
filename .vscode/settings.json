{
  "files.exclude": {
     "**/*.js": {"when": "$(basename).ts"},
     "**/*.d.ts": {"when": "$(basename).ts"},
  },
  "resolveSourceMapLocations": [
    "${workspaceFolder}/**",
    "!**/node_modules/**"
  ]
}